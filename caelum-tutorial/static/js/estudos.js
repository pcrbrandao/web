// JavaScript Documentfunction atualizaId(id, string) {	var elemento = document.getElementById(id);	elemento.innerHTML = string;}function impares(inicio, fim) {	var texto = "";	for (i = inicio; i < fim; i++) {		if(i % 2 != 0)			texto += i + " ";	}		return texto;}function numeroParaReal(numero) {		var formatado = numero.toFixed(2);	formatado = "R$ " + formatado;		return formatado.replace(".", ",");}function realParaNumero(string) {	var compativelComParseFloat = string.replace("R$ ", "");		return parseFloat(compativelComParseFloat.replace(",", "."));}